'use client'

/**
 * Re-export del sistema Contextual Toast.
 * Sustituye al sistema anterior de toasts.
 */
export { useContextualToast as useToast, useContextualToast } from '@/components/contextual-toast'
